/*
$( document ).ready(function() {
    $('#mosaic').Mosaic({
        refitOnResize: true,
        maxRowHeightPolicy: 'oversize', // skip | crop | oversize
        maxRowHeight: 300,
        defaultAspectRatio: 1,
    });
});
*/